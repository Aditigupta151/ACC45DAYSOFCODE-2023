#include <stdio.h>

int main() {

   int t;
   cin>>t;
   while(t--){
     int x,y,z;
      cin>>x>>y>>z;
      int total = (x*5) + (y*10);
       int c = total/z;
      cout<<c<<end1;
      }
      return 0;
}
 
